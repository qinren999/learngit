eureka:
  client:
    service-url:
      #注册中心地址
      defaultZone: http://localhost:8080/eureka
spring:
  application:
    #服务在注册中心的名称 不同服务名称不能重复
    name: cloud-feign
  cloud:
    config:
      discovery:
        service-id: cloud-config
        enabled: true
      profile: prod
        #配置文件所属git 分支
      label: master

